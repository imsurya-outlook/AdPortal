@model double
@{
    var rounded = (int)Math.Round(Model, MidpointRounding.AwayFromZero);
}
<span class="rating-stars">
    @for (var i = 1; i <= 5; i++)
    {
        <i class="bi @(i <= rounded ? "bi-star-fill" : "bi-star")"></i>
    }
</span>
